# Thread 3: Pool Control + Execution Loop
# pip install -r requirements.txt

# gRPC
grpcio>=1.60.0
grpcio-tools>=1.60.0

# Cryptography
pynacl>=1.5.0
blake3>=0.4.0

# Claude API (optional - for agent generation)
anthropic>=0.20.0

# Shared with Thread 2 (for RAG integration)
numpy>=1.24.0
sentence-transformers>=2.2.0  # optional - falls back to hash embeddings
